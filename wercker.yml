box: vvakame/review

build:
  steps:
    - script:
        name: Build PDF
        code: |
          cd unbalance
          review pdfmaker config.yml
          ../upload_dropbox.sh book.pdf ${DROPBOX_TOKEN}
